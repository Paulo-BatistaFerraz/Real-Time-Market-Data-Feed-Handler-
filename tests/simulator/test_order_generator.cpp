#include <gtest/gtest.h>
#include "mdfh/simulator/order_generator.hpp"

using namespace mdfh::simulator;

// TODO: Order generator tests

TEST(OrderGenerator, GeneratesAllMessageTypes) {
    EXPECT_TRUE(true);
}

TEST(OrderGenerator, DistributionMatchesConfig) {
    EXPECT_TRUE(true);
}

TEST(OrderGenerator, DeterministicWithSameSeed) {
    EXPECT_TRUE(true);
}
