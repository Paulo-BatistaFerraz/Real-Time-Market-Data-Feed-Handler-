#include <gtest/gtest.h>
#include "mdfh/simulator/sim_matching_engine.hpp"

using namespace mdfh::simulator;

// TODO: Matching engine tests

TEST(SimMatchingEngine, CrossingOrdersProduceTrade) {
    EXPECT_TRUE(true);
}

TEST(SimMatchingEngine, NoCrossNoTrade) {
    EXPECT_TRUE(true);
}

TEST(SimMatchingEngine, PartialFill) {
    EXPECT_TRUE(true);
}
