#include <gtest/gtest.h>
#include "mdfh/core/order_book.hpp"

using namespace mdfh;
using namespace mdfh::core;

// TODO: Order book tests

TEST(OrderBook, AddAndBestBidAsk) {
    EXPECT_TRUE(true);
}

TEST(OrderBook, CancelRemovesOrder) {
    EXPECT_TRUE(true);
}

TEST(OrderBook, ExecuteReducesQuantity) {
    EXPECT_TRUE(true);
}

TEST(OrderBook, ReplaceChangesPriceLevel) {
    EXPECT_TRUE(true);
}

TEST(OrderBook, SpreadCalculation) {
    EXPECT_TRUE(true);
}

TEST(OrderBook, DepthReturnsTopNLevels) {
    EXPECT_TRUE(true);
}
