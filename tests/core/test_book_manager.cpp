#include <gtest/gtest.h>
#include "qf/core/book_manager.hpp"

using namespace qf;
using namespace qf::core;

// TODO: Book manager tests

TEST(BookManager, RoutesToCorrectSymbol) {
    EXPECT_TRUE(true);
}

TEST(BookManager, AutoCreatesBookOnFirstMessage) {
    EXPECT_TRUE(true);
}

TEST(BookManager, TracksMultipleSymbols) {
    EXPECT_TRUE(true);
}

TEST(BookManager, ResetClearsAllBooks) {
    EXPECT_TRUE(true);
}
