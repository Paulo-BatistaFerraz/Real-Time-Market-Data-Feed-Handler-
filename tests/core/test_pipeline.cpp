#include <gtest/gtest.h>
#include "qf/core/pipeline.hpp"

using namespace qf::core;

// TODO: Pipeline integration tests

TEST(Pipeline, StartsAndStops) {
    EXPECT_TRUE(true);
}

TEST(Pipeline, DataFlowsEndToEnd) {
    EXPECT_TRUE(true);
}
