#include <gtest/gtest.h>
#include "mdfh/protocol/frame_parser.hpp"

using namespace mdfh::protocol;

// TODO: Frame parser tests

TEST(FrameParser, ParsesMultiMessageDatagram) {
    EXPECT_TRUE(true);
}

TEST(FrameParser, HandlesSingleMessage) {
    EXPECT_TRUE(true);
}

TEST(FrameParser, RejectsTruncatedDatagram) {
    EXPECT_TRUE(true);
}
