#include <gtest/gtest.h>
#include "mdfh/protocol/batcher.hpp"

using namespace mdfh::protocol;

// TODO: Batcher tests

TEST(Batcher, FlushesWhenFull) {
    EXPECT_TRUE(true);
}

TEST(Batcher, FlushesOnExplicitCall) {
    EXPECT_TRUE(true);
}

TEST(Batcher, RejectsTooLargeMessage) {
    EXPECT_TRUE(true);
}

TEST(Batcher, BatchHeaderHasCorrectSeqNum) {
    EXPECT_TRUE(true);
}
