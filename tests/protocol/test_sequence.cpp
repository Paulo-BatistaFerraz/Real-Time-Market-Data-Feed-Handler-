#include <gtest/gtest.h>
#include "mdfh/protocol/sequence.hpp"

using namespace mdfh::protocol;

// TODO: Sequence tracker tests

TEST(SequenceTracker, DetectsGap) {
    EXPECT_TRUE(true);
}

TEST(SequenceTracker, DetectsDuplicate) {
    EXPECT_TRUE(true);
}

TEST(SequenceTracker, AcceptsSequentialPackets) {
    EXPECT_TRUE(true);
}

TEST(SequenceTracker, HandlesWraparound) {
    EXPECT_TRUE(true);
}
