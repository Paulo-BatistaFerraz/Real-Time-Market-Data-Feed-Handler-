The executable exists and built successfully. The exit code 127 seems to be an environment issue (likely missing DLLs in PATH), not a build problem. The build itself passes cleanly.

## Acceptance Criteria

- **`EventBus` allows subscribing callbacks to event types** ΓÇö PASS: `subscribe<EventType>(callback)` stores type-erased callbacks in a map keyed by `std::type_index`
- **`subscribe<EventType>(callback)` registers a listener** ΓÇö PASS: Implemented as a template method that adds to a `SubscriberList<EventType>`
- **`publish(event)` dispatches to all registered listeners for that event type** ΓÇö PASS: Looks up by `typeid`, iterates all callbacks for that type
- **Thread-safe: subscriptions at startup, publishing from any thread** ΓÇö PASS: `subscribe()` takes `std::unique_lock` (exclusive), `publish()` takes `std::shared_lock` (concurrent reads)
- **Uses `std::function` for callbacks, `std::unordered_map` by `typeid` for dispatch** ΓÇö PASS: `std::function<void(const EventType&)>` for callbacks, `std::unordered_map<std::type_index, ...>` for dispatch
- **Build passes** ΓÇö PASS: All targets built successfully
