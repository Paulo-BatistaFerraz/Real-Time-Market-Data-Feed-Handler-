All implemented. Here's the acceptance criteria assessment:

**Acceptance Criteria:**

- **`SimOrderBook` tracks live orders per symbol (order_id ΓåÆ {symbol, side, price, qty})** ΓÇö PASS. `orders_` is `unordered_map<OrderId, core::Order>` which stores all fields.

- **`add_order()` inserts order, returns order_id** ΓÇö PASS. `add()` inserts into the map and id vector, returns `true` on success (order_id is in the Order struct).

- **`get_random_order()` returns a random live order ID** ΓÇö PASS. `random_live_order(rng)` picks uniformly from `order_ids_`; side-filtered overload also provided.

- **`remove_order(id)` removes order** ΓÇö PASS. `cancel(id)` removes the order and returns it; `execute(id, qty)` removes when fully filled.

- **`SimMatchingEngine` detects when a new order crosses the book (buy price >= best ask, or sell price <= best bid)** ΓÇö PASS. `try_match()` loops checking `best_bid_order()` vs `best_ask_order()`, crosses when `bid->price >= ask->price`.

- **On cross: generates a TradeMessage, removes/reduces the matched orders** ΓÇö PASS. Creates `TradeMessage` with symbol, price, quantity, and both order IDs, then calls `execute()` on both sides to reduce/remove.

- **Build passes** ΓÇö PASS. All targets build cleanly, 62 tests pass.
