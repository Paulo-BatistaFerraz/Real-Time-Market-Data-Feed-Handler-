#include "qf/oms/order_id_generator.hpp"

// OrderIdGenerator is fully inline (atomic operations in the header).
// This translation unit exists to satisfy the build system and
// ensure the header compiles cleanly as a standalone unit.

namespace qf::oms {
}  // namespace qf::oms
